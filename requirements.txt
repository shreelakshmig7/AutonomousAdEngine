# ------------------------------------------------------------------------------
# Python: 3.10+ required (google.api_core and stack drop 3.9; Streamlit Cloud: use 3.10–3.12).
# Install: python3.10 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# ------------------------------------------------------------------------------

# AI Framework & APIs
google-generativeai==0.8.3      # Gemini 2.0 Flash (Drafter) — Google API key
google-genai==1.47.0            # Gemini 2.5 Flash Image only (images/image_generator.py); pinned exact
anthropic==0.69.0               # Claude Sonnet 4.5 (Judge) — satisfies langchain-anthropic>=0.69.0
python-dotenv==1.0.1            # Secure API key loading from .env

# Data & Structured Output
pydantic==2.11.2                 # Strict JSON schema validation for all LLM output
pandas==2.2.2                   # Evaluation reports + iteration_log.csv exports

# Visualization
matplotlib==3.9.0               # quality_trends.png chart (bonus points)
seaborn==0.13.2                 # Polished chart styling

# Resilience
tenacity==8.3.0                 # Exponential backoff + fallback model on rate limits

# CLI Output
rich==13.7.1                    # Live pipeline progress for demo video

# Streamlit Cloud
streamlit==1.40.2               # app.py dashboard + pipeline runner
plotly==5.24.1                  # Radar, bar, line charts in app.py (better than matplotlib in Streamlit)

# Testing
pytest==8.2.0                   # Core test framework — 12 required tests
pytest-mock==3.14.0             # Mock all API calls — tests run fully offline
